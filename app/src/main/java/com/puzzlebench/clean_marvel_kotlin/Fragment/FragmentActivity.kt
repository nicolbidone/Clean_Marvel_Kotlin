package com.puzzlebench.clean_marvel_kotlin.Fragment

/*
class FragmentActivity(character: Int, activity: MainActivity) : BaseRxActivity() {
    val getSingleCharacterServiceUseCase = GetSingleCharacterServiceUseCase(CharacterServicesImpl(), character)
    val presenter = FragmentPresenter(FragmentView(activity), getSingleCharacterServiceUseCase, subscriptions)

    init {
        presenter.init()
    }
}*/
